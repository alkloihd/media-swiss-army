# Dependencies
node_modules/
node_modules
node_modules.nosync/

# System / logs
.DS_Store
*.log

# Environment / secrets
.env
.env.*
.env.nosync

# Local dev artifacts (MCP, test tooling, screenshots)
.playwright-mcp/
.superpowers/
screenshots/

# Local Claude Code config (permissions, plans)
# Note: .claude/settings.local.json is the standard local-only file.
# .claude/settings.json here is also local — not tracked upstream.
.claude/settings.json
.claude/settings.local.json
.claude/plans/
.claude/worktrees/

# Agent session activity logs (raw shell-history per session) only.
# All planning docs, AI-CHAT-LOG.md, HANDOFF.md, PLAN-*, AUDIT-*, BACKLOG-*
# under .agents/work-sessions/ ARE tracked — they're the project's
# decision history and need to be reachable from phone via github.com.
.agents/work-sessions/**/session-activity.log

# Docs working area (chat exports, drafts — may contain personal info)
# Existing tracked Docs/*.md files remain in history; this blocks new additions.
Docs/
